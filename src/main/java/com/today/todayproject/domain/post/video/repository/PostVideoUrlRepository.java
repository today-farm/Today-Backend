package com.today.todayproject.domain.post.video.repository;

import com.today.todayproject.domain.post.video.PostVideoUrl;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PostVideoUrlRepository extends JpaRepository<PostVideoUrl, Long> {
}
